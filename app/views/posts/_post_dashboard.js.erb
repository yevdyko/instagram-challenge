$('.posts').append('<%=j render 'posts' %>');
$('.posts-pagination').html('<%=j link_to_next_page(@posts, 'Load more', remote: true, class: 'load-more') %>');
if (!$('.load-more').length) { $('.posts-pagination').remove(); }
